# ============================================================
# IT Procurement Automation — Python Dependencies
# ============================================================
# Install with: pip install -r requirements.txt
# Requires Python >= 3.10
#
# For development install run:
#   pip install -e ".[dev]"

# ------------------------------------------------------------
# Jira integration
# Uses REST API v3 — configure api_version in config.yaml
# ------------------------------------------------------------
jira>=3.5.0,<4.0.0

# ------------------------------------------------------------
# Configuration & environment
# ------------------------------------------------------------
pyyaml>=6.0.0
python-dotenv>=1.0.0

# ------------------------------------------------------------
# HTTP requests (Netsuite, notifications)
# ------------------------------------------------------------
requests>=2.28.0

# ------------------------------------------------------------
# Email notifications
# ------------------------------------------------------------
secure-smtplib==0.1.1

# ------------------------------------------------------------
# PDF handling
# Optional — used for internal reports in future versions
# Vendor PDF is downloaded directly from portal
# ------------------------------------------------------------
fpdf2>=2.7.0

# ------------------------------------------------------------
# Web scraping — vendor portal (live mode only)
# After installing run: playwright install chromium
# ------------------------------------------------------------
playwright>=1.40.0

# ------------------------------------------------------------
# Netsuite authentication (live mode only)
# ------------------------------------------------------------
requests-oauthlib>=1.3.0

# ------------------------------------------------------------
# CLI — terminal formatting and loading indicators
# ------------------------------------------------------------
rich>=13.0.0